<%= form_for(@topic) do |f| %>

  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.hidden_field :name, :value => "#{current_user.name}"  %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
